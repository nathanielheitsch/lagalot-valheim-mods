<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- Rename this folder + these three to make a new mod.
         Everything else (JotunnLib, BepInEx, game assemblies, net48 target)
         is inherited from Directory.Build.props. -->
    <AssemblyName>LagalotTemplate</AssemblyName>
    <RootNamespace>LagalotTemplate</RootNamespace>
    <Version>0.0.1</Version>
  </PropertyGroup>

</Project>
